## What changed

<!-- One or two lines. The roadmap step if it maps to one, e.g. "1c — mirror the aggregate contract". -->

## Why

<!-- The problem this solves. Skip if it's obvious from the title. -->

## Validation

- [ ] `npm run typecheck` passes
- [ ] Checked in the browser against a running backend

<!-- `npm run dev` does NOT typecheck — Vite only transpiles. The typecheck is
     the only thing that catches drift between types/api.ts and the backend. -->

## Notes

<!-- Anything deliberately left out, follow-ups created, or decisions worth
     recording in CLAUDE.md. -->
